<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Akiroute.Views.Dialogs.EditNodeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="编辑节点"
    PrimaryButtonText="保存"
    CloseButtonText="取消"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick">

    <ScrollViewer MaxHeight="480" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="12" MinWidth="420">
            <TextBox
                x:Name="NameBox"
                Header="名称"
                PlaceholderText="节点显示名称" />

            <ComboBox
                x:Name="TypeBox"
                Header="类型"
                PlaceholderText="协议类型" />

            <TextBox
                x:Name="AddressBox"
                Header="地址"
                PlaceholderText="服务器主机名或 IP" />

            <TextBox
                x:Name="PortBox"
                Header="端口"
                PlaceholderText="1 - 65535" />

            <!--
                Protocol-specific fields (uuid, sni, password, ...) held as raw JSON.
                Round-tripped with System.Text.Json.Nodes only — no reflection.
            -->
            <Expander
                x:Name="ExtraExpander"
                Header="高级参数 (ExtraParams)">
                <TextBox
                    x:Name="ExtraParamsBox"
                    Height="140"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    FontFamily="Consolas"
                    FontSize="12"
                    PlaceholderText="{}{ }" />
            </Expander>

            <TextBlock
                x:Name="ValidationText"
                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                TextWrapping="Wrap"
                Visibility="Collapsed" />
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
